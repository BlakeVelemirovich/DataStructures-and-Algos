var characterReplacement = function(s, k) {
    let windowStart = 0;
    let currentMax = 0;
    let hashMap = {};
    let counter = 0;

    for (i = 0; i < s.length; i++) {
        if (counter = 2) {
            hashMap[s[windowStart]]++;
            counter--;
            windowStart++;
        }

        hashMap[windowEnd] ? hashMap[windowEnd] = 2 : hashMap[windowEnd]++;
    }
};